정상 동작하는 환경을 저장해 두었다가 선택해서 사용하는 방식은 Docker 없이도 구현할 수 있습니다. Linux에는 파일시스템을 겹쳐 보여 주는 OverlayFS, 파일시스템 스냅샷, 시스템 확장 이미지를 연결·해제하는 `systemd-sysext` 같은 기능이 있습니다. (Kernel Documentation)

다만 다음 두 주장은 구분해야 합니다.

> **“검증된 프로젝트 환경을 보존하고 전환할 수 있다.” → 가능.“Ubuntu와 ROS의 모든 구성은 그대로 두면서, 나머지 어떤 패키지든 자유롭게 설치한 환경을 전환할 수 있다.” → 일반적으로 보장할 수 없음.**
> 

핵심은 이미지 파일을 만드는 기술보다 **무엇을 고정하고, 무엇을 함께 전환해야 환경의 일관성이 유지되는가**입니다.

---

## 1. 먼저, 이 방식으로 줄일 수 있는 일이 실제로 있다

다음 상황을 가정하겠습니다.

프로젝트 A는 특정 라이브러리 조합에서 정상 동작하고, 프로젝트 B는 다른 조합에서 정상 동작합니다. 현재는 하나의 설치 환경을 계속 수정하면서 A와 B를 번갈아 사용합니다.

이때 **A와 B의 정상 상태를 각각 온전히 보존하고 정확히 복원할 수 있다면, 전환할 때마다 같은 의존성 충돌을 다시 해결할 이유는 없습니다.** 스냅샷과 별도 실행 이미지가 제공하는 기능을 적용하면 얻을 수 있는 직접적인 효과입니다. (Btrfs Documentation)

“결국 의존성 오류를 해결해야 하므로 시간이 줄어들지 않는다”는 설명은 다음처럼 나누어야 합니다. 통화에는 최초 환경 구축과 이미 구축된 환경으로 돌아가는 작업이 함께 논의되었습니다.

| 작업 | 이미지화·스냅샷이 제공하는 효과 |
| --- | --- |
| **새 프로젝트의 환경을 처음 정상화** | 필요한 호환성 검토와 의존성 해결은 여전히 해야 함 |
| **이미 정상화된 A에서 이미 정상화된 B로 전환** | 보존 범위가 충분하다면, 이전에 했던 설치·조정 작업을 반복하지 않아도 됨 |
| **설치나 수정으로 망가진 환경을 정상 상태로 복구** | 원인을 모두 역추적하지 않고 정상 상태로 돌아갈 수 있음 |
| **기존 환경에 새 장비나 새 모듈을 추가** | 새로운 조합의 검증은 필요하지만, 실패했을 때 되돌아갈 기준점을 제공 |

**최초 문제 해결을 없애는 방식은 아니지만, 해결한 문제를 다시 해결하는 비용을 줄이는 방식으로는 의미가 있습니다.**

---

## 2. “Ubuntu + ROS를 고정한다”의 범위가 중요하다

### 버전 이름만 고정하는 것과 실제 실행 환경을 고정하는 것은 다르다

`Ubuntu 22.04 + ROS 2 Humble`이라는 이름이 같더라도, 실행에 사용되는 Python 패키지와 공유 라이브러리 구성이 다르면 프로그램 동작 조건은 달라집니다. 특히 ROS 패키지가 빌드 당시 가정한 라이브러리의 ABI, 즉 바이너리 간 연결 규약이 바뀌면 런타임 문제가 발생할 수 있습니다. colcon 공식 문서도 이 문제를 명시합니다. (Colcon)

따라서 고정 기반은 단순히 다음 두 폴더처럼 나눌 수 있는 대상이 아닙니다.

```
Ubuntu 폴더 → 고정
ROS 폴더    → 고정
나머지      → 전환
```

실제로는 ROS와 프로젝트가 같은 Python 패키지나 시스템 라이브러리를 사용할 수 있기 때문입니다.

설계상으로는 다음처럼 정의하는 편이 정확합니다.

```
공통 기반 B
  = 고정된 OS 구성 + ROS + 공통으로 사용하는 의존성

프로젝트 A 환경
  = B + A 전용 파일·패키지·설정

프로젝트 B 환경
  = B + B 전용 파일·패키지·설정
```

**A와 B가 동일한 B와 호환된다면, 이 구조는 성립합니다.**

반대로 B에 포함된 라이브러리를 A는 반드시 교체해야 하고, B 프로젝트는 그 교체를 허용하지 않는다면, 그 라이브러리를 정말 공통 기반에 둘 수 있는지부터 다시 결정해야 합니다. 이는 이미지 기술의 한계가 아니라 **공통으로 고정한 범위가 실제 의존성과 맞지 않는 문제**입니다.

### 원본 파일을 보존하는 것과 실행 시 같은 파일을 사용하는 것도 다르다

OverlayFS에서는 아래쪽의 원본 파일을 수정하지 않고, 위쪽 계층의 다른 파일을 실행 프로그램에 보여 줄 수 있습니다. 따라서 **“원본 Ubuntu 파일은 그대로 보존했다”면서도 “프로젝트 실행 시에는 다른 버전의 라이브러리를 사용한다”는 구성이 가능합니다.** (Kernel Documentation)

이 방식은 저장 공간상의 공통 기반을 유지하는 데 유용합니다. 다만 이를 **모든 프로젝트가 동일한 OS 라이브러리를 사용한다는 의미의 고정**과 혼동해서는 안 됩니다.

---

## 3. Workspace와 Python 환경은 비교적 쉽고, `apt`까지 포함하면 관리 범위가 커진다

### 3.1. Workspace + Python 환경 + 별도 경로에 설치한 SDK

프로젝트 전용 구성을 다음처럼 모을 수 있다면 비교적 단순합니다.

```
/opt/lab/projects/A/
    workspace/
    venv/
    sdk/
    config/

/opt/lab/projects/B/
    workspace/
    venv/
    sdk/
    config/
```

A를 실행할 때 A의 구성과 라이브러리를 선택하고, B를 실행할 때 B의 구성을 선택하는 방식입니다. ROS는 여러 워크스페이스의 연결을 지원하며, Linux의 동적 링커도 프로그램별 라이브러리 검색 경로를 사용할 수 있습니다. **따라서 Python 이외의 C/C++ 라이브러리도, 설치·링크 방식이 허용하면 프로젝트별로 분리할 수 있습니다.** (Colcon)

이 구성이 가능하다면 매번 압축을 풀거나 덮어쓸 필요도 없습니다. 프로젝트별 디렉터리를 유지하면서 실행 대상을 선택하고, 별도의 아카이브나 스냅샷은 보존·복구용으로 사용할 수 있습니다.

다만 주의할 점이 있습니다.

**첫째, ROS가 Python 가상환경을 원천적으로 사용할 수 없다는 설명은 틀립니다.** ROS 2 Humble 공식 문서는 가상환경을 사용하는 절차를 제공합니다. 중요한 조건은 배포된 ROS 바이너리와 호환되는 Python 인터프리터를 사용하는 것입니다. 시스템 Python과 다른 인터프리터를 사용하는 Conda 환경에는 별도의 호환성 주의가 필요합니다.

**둘째, 가상환경 디렉터리를 아무 경로로나 옮겨도 되는 것은 아닙니다.** Python 가상환경에는 인터프리터와 스크립트의 절대 경로 등이 들어갈 수 있으므로, 공식 문서도 일반적인 이동·복사를 전제로 하지 않습니다. 보존한 환경을 재사용하려면 원래 경로를 유지하거나, 새 위치에서 재구성하도록 설계해야 합니다. (Python documentation)

**셋째, 워크스페이스 분리는 완전한 의존성 격리가 아닙니다.** 서로 다른 워크스페이스를 연결했더라도, 공통 라이브러리를 비호환 버전으로 덮어 사용하면 문제가 생길 수 있습니다. (Colcon)

**판정:** 필요한 의존성을 이 범위에 수용할 수 있다면, 현실적인 선택입니다. 이미지 전환보다 더 단순한 “환경 선택”으로 목적을 달성할 수도 있습니다.

### 3.2. `apt`로 설치한 패키지는 왜 더 까다로운가

`apt`로 설치한 패키지는 프로젝트 폴더 하나에만 존재하지 않습니다. 설치된 실행 파일과 라이브러리 외에, 설정 파일과 패키지 관리 기록이 관련됩니다. 패키지는 설치·제거 과정에서 `preinst`, `postinst` 등의 스크립트를 실행할 수도 있습니다. (Debian)

예를 들어 보존 범위를 검토할 때는 다음과 같은 영역을 고려해야 합니다. **모든 패키지가 이 경로들을 전부 수정한다는 뜻은 아닙니다.**

```
/usr/...              실행 파일·라이브러리 등
/etc/...              설정 및 라이브러리 검색 관련 구성
/var/lib/dpkg/...      설치 패키지 상태와 관리 정보
/opt/...               일부 외부 소프트웨어
사용자 홈 디렉터리      프로그램별 사용자 설정
```

특히 `dpkg`는 `/var/lib/dpkg`에 설치 상태를 관리하며, 일부 설정은 패키지 스크립트가 별도로 생성·관리할 수 있습니다. 따라서 실행 파일만 A 버전으로 되돌리고 패키지 관리 기록은 B 상태로 남겨 놓으면, 실제 파일과 관리자가 인식하는 상태가 어긋납니다. (Ubuntu Manpages)

여기서 중요한 결론은 다음입니다.

> **`apt` 패키지이므로 이미지화가 불가능한 것이 아닙니다.
실행 파일뿐 아니라 설치 상태와 관련 설정까지 일관되게 보존해야 한다는 것입니다.**
> 

또한 **SDK가 `/usr/local` 같은 시스템 경로에 설치된다는 사실 자체도 불가능의 근거는 아닙니다.** 그 경로까지 포함하는 파일시스템 이미지를 사용할 수 있습니다. 진짜 문제는 해당 설치가 다른 공통 파일, 시스템 서비스, 실행 중인 커널 상태까지 어디에 영향을 미치는가입니다.

반대로 기존 PC에서 `/usr/lib`나 일부 Python 폴더만 골라 압축하고, 이를 번갈아 덮어쓰는 방식은 권하지 않습니다. 누락된 설정, 남아 있는 이전 파일, 패키지 관리 정보 불일치를 직접 책임져야 하기 때문입니다.

---

## 4. Docker 없이 실제로 사용할 수 있는 구현 방식

다음은 같은 목적을 달성하는 서로 다른 방식입니다. **어느 방식도 모든 범위에서 우월하지 않습니다.**

| 방식 | 주로 전환하는 범위 | 현실적인 평가 |
| --- | --- | --- |
| **프로젝트별 디렉터리·실행 환경 선택** | Workspace, Python 패키지, 분리 가능한 SDK·라이브러리 | 분리 범위가 작을 때 가장 단순 |
| **시스템 확장 이미지: `systemd-sysext`** | 검증된 `/usr`, `/opt` 추가 구성 | 질문의 구상과 가깝지만, 임의의 `apt` 환경 전체를 다루는 도구는 아님 |
| **별도 루트 파일시스템 + 실행 환경 분리** | 프로젝트별 사용자 공간과 패키지 관리 상태 | `apt`까지 폭넓게 분리 가능. 장치·권한 등의 구성 부담 존재 |
| **파일시스템 스냅샷·부팅 환경 전환** | 시스템 전역의 일관된 설치 상태 | 전역 변경이 많을 때 유리하지만, 범위에 따라 재부팅·오프라인 복원 필요 |

### 4.1. `systemd-sysext`: 질문과 직접적으로 닮은 기능이 실제로 있다

Ubuntu 22.04의 `systemd-sysext`는 이미지 안의 `/usr`와 `/opt`를 호스트의 같은 경로에 겹쳐 연결합니다. 해제하면 기존 호스트 파일이 다시 보입니다. 디렉터리뿐 아니라 squashfs나 ext4 기반 파일 이미지도 사용할 수 있습니다. **Docker도 아니고, 별도의 OS를 부팅하는 가상머신도 아닙니다.** (Ubuntu Manpages)

따라서 “고정된 기반에 추가 환경을 이미지 형태로 연결했다가 빼는 것이 원리적으로 가능한가?”에 대한 직접적인 답은 **그렇다**입니다.

그러나 Ubuntu 22.04에 해당하는 문서 기준으로, 이미지 내부의 `/etc`와 `/var`는 이 기능으로 연결되지 않습니다. 또한 의존성을 자동 해결하는 일반 패키지 관리 체계가 아니며, 필요한 파일은 기반 OS에 있거나 이미지가 직접 제공해야 합니다. (Ubuntu Manpages)

**판정:** 검증된 SDK와 도구를 추가하는 용도로는 후보가 됩니다. 하지만 “평소처럼 `apt install`한 결과 전체를 자동으로 포장해서 교체한다”는 요구에 바로 대응하는 범용 해답은 아닙니다.

### 4.2. 별도 루트 파일시스템: `apt`까지 포함하려면 더 명확한 경계

프로젝트마다 프로그램이 보는 `/usr`, `/etc`, `/var/lib/dpkg` 등을 따로 두면, 시스템 경로에 설치되는 패키지도 해당 환경 안에서 관리할 수 있습니다.

`chroot` 계열이나 `systemd-nspawn`이 이런 접근에 사용됩니다. `systemd-nspawn`은 디렉터리 또는 디스크 이미지를 루트 파일시스템으로 사용해 명령을 실행할 수 있고, 스냅샷과 읽기 전용 기반의 실행도 지원합니다. (Ubuntu Manpages)

다만 두 가지를 명확히 해야 합니다.

**첫째, Docker를 사용하지 않더라도 `systemd-nspawn`은 운영체제 수준의 컨테이너입니다.** 이름만 다른 순수 파일 교체 방식인 것처럼 설명해서는 안 됩니다.

**둘째, 이미지 안에는 Ubuntu 사용자 공간 구성도 포함됩니다.** 호스트 Ubuntu를 그대로 둔다는 조건은 만족할 수 있지만, “Ubuntu 관련 파일은 전혀 포함하지 않고 프로젝트 파일만 보관한다”는 의미와는 다릅니다. 공통 파일을 스냅샷이나 계층으로 공유해 저장 중복을 줄일 수는 있습니다.

장치 접근과 네트워크도 별도 검토 대상입니다. 예를 들어 `systemd-nspawn`은 네트워크 공유가 가능하지만, 실행 방식과 옵션에 따라 기본 구성이 달라집니다. 따라서 **Docker를 제외했다는 이유만으로 기존의 네트워크·권한 문제가 자동으로 없어지지는 않습니다.** (Ubuntu Manpages)

**판정:** `apt`와 SDK 설치 상태까지 프로젝트별로 보존하려면 기술적으로 정돈된 접근입니다. 다만 이미지를 만드는 작업 외에 실행 환경을 운영하는 체계가 필요합니다.

### 4.3. 파일시스템 스냅샷: 전역 변경이 많다면 더 단순할 수 있다

프로젝트 설치가 여러 시스템 경로에 퍼져 있다면, 이를 억지로 잘게 분리하는 것보다 **정상 상태의 파일시스템을 일관된 단위로 보존하는 방식**이 더 관리하기 쉬울 수 있습니다.

Btrfs 스냅샷은 원본과 데이터 블록을 공유하는 방식이므로, 스냅샷을 만들 때 모든 파일을 다시 복사하는 구조는 아닙니다. 따라서 **환경 전환이 반드시 전체 재설치나 대용량 파일 복사를 뜻하지는 않습니다.** (Btrfs Documentation)

다만 실행 중인 호스트 시스템 전체를 바꾸는 경우에는, 새 상태로 부팅하거나 오프라인에서 복원하는 운영이 더 명확합니다. 이는 “Ubuntu와 ROS를 계속 켜 둔 채 나머지만 교체한다”는 엄격한 조건에서는 벗어납니다.

또한 스냅샷 범위가 중요합니다. Btrfs의 중첩 서브볼륨은 상위 스냅샷에 자동으로 포함되지 않으므로, 패키지 파일은 복원했지만 설치 기록은 복원하지 않는 구성이 되지 않도록 해야 합니다. (Btrfs Documentation)

**판정:** 시스템 전역 변경이 많고 재부팅을 허용할 수 있다면 충분히 현실적입니다. 부분 분리 시스템을 새로 만드는 것보다 오히려 단순할 수 있습니다.

---

## 5. 어떤 경우에는 “고정 기반 + 나머지 전환” 조건을 만족할 수 없는가

### 5.1. 프로젝트가 고정한 기반의 변경을 반드시 요구하는 경우

가상의 예로 다음 조건을 놓겠습니다.

```
고정 기반:
  기존 ROS 바이너리가 라이브러리 L의 특정 ABI를 필요로 함

새 프로젝트:
  그와 호환되지 않는 L을 같은 실행 구성에서 반드시 사용해야 함
```

프로젝트의 실행 구조를 그대로 유지하면서 두 요구가 실제로 양립하지 않는다면, 이미지를 저장하는 것으로 해결되지는 않습니다. 의존 패키지를 재빌드하거나, 프로세스·실행 환경을 분리하거나, 고정 기반을 바꾸어야 합니다. colcon도 ABI를 변경하는 패키지 재정의에 대해 의존 패키지의 재빌드를 요구합니다. (Colcon)

단, **버전이 다르다는 것만으로 충돌이라고 단정하면 안 됩니다.** 서로 다른 프로세스에서 사용하거나 서로 구분되는 라이브러리로 공존할 수 있는 경우도 있으므로, 실제 로딩 구조를 봐야 합니다.

### 5.2. 실행 중인 커널이나 커널 드라이버를 바꿔야 하는 경우

사용자 공간의 SDK와 커널 드라이버는 구분해야 합니다.

프로젝트 이미지에 SDK 파일을 보존하는 것과, 현재 실행 중인 커널·드라이버를 그 이미지에 맞는 상태로 바꾸는 것은 다른 작업입니다. 예를 들어 NVIDIA 드라이버에는 사용자 공간 구성과 커널 구성요소가 모두 있고, CUDA 애플리케이션에는 호환되는 드라이버가 필요합니다. (NVIDIA Docs)

따라서 고정한 드라이버에서 지원되지 않는 기능을 프로젝트가 반드시 요구한다면, **그 프로젝트의 사용자 공간 파일만 바꿔서는 조건을 충족하지 못합니다.**

반대로 호환성 범위 내에서 서로 다른 CUDA 런타임이나 라이브러리를 사용하는 경우까지 모두 불가능한 것은 아닙니다. 이 역시 실제 요구 버전과 호환성을 확인해야 합니다. (NVIDIA Docs)

### 5.3. 저장 대상 밖의 상태가 문제인 경우

PC의 파일 이미지에 포함하지 않은 장비 내부 설정, 펌웨어 상태, 실제 케이블 연결, 장비의 일시적인 이상 상태까지 파일 복원이 되돌려 주지는 않습니다.

이는 이미지 방식의 결함이라기보다 보존 범위의 문제입니다. 따라서 이 방식으로 줄이려는 문제가 **설치 환경의 오염인지, 장비 연결·운영상 문제인지**를 먼저 구분해야 합니다.

---

## 6. 반복 전환에 사용하려면 지켜야 할 운영 조건

### 이전 환경에 새 환경을 계속 덧붙이면 안 된다

공통 기반을 `B`, 프로젝트별 변경분을 `ΔA`, `ΔB`라고 하면 목표는 다음입니다.

```
A 실행: B + ΔA
B 실행: B + ΔB
```

다음이 되어서는 안 됩니다.

```
B 실행: B + ΔA + ΔB
```

후자는 A에서 설치한 파일과 설정이 남는 현재의 문제를 그대로 반복할 수 있습니다. 파일시스템 계층에는 추가·수정뿐 아니라 삭제 상태도 표현되어야 하며, OverlayFS가 삭제 표시인 whiteout을 사용하는 이유도 여기에 있습니다. (Kernel Documentation)

### “파일 선택 변경”과 “실험 재개”는 별개의 단계다

제가 권하는 전환 절차는 다음과 같습니다.

```
진행 중인 실험을 안전하게 종료
→ 기존 프로젝트의 관련 프로세스·서비스 종료
→ 프로젝트 환경 전환
→ 새 실행 환경에서 시작
→ 장비 연결과 기본 데이터 수신 확인
→ 실험 재개
```

이것은 **실행 중인 로봇 제어 프로그램을 끊지 않고 의존성만 바꾸는 무중단 교체**와는 다른 목표입니다. 특히 시스템 확장 이미지의 연결 갱신도 완전히 원자적인 실행 상태 교체는 아니며, Ubuntu 22.04 문서에는 갱신 중 확장 파일이 잠시 사라지는 구간이 명시되어 있습니다. (Ubuntu Manpages)

### 환경과 연구 결과는 분리하는 편이 낫다

운영 설계상으로는 다음처럼 나누는 것을 권합니다.

```
버전 관리·복원 대상:
  실행 코드, 패키지, SDK, 실행 설정

별도 보존 대상:
  실험 로그, rosbag, 데이터셋, 학습 결과, 아직 저장하지 않은 작업

별도 검증 대상:
  장비별 캘리브레이션, 장비 식별 정보, 네트워크·권한 설정
```

특히 캘리브레이션은 무조건 공용 또는 무조건 프로젝트 전용으로 정할 것이 아니라, **그 환경이 어떤 실제 장비 구성에 대해 검증되었는지**와 연결해 관리해야 합니다.

---

## 7. 실제로 유용한지는 어떻게 판단해야 하는가

다음은 위 기능과 제약에 따른 운영상 판단입니다.

| 현재 연구실의 상황 | 기대되는 효용 |
| --- | --- |
| 동일한 PC에서 **이미 검증된 몇 개의 프로젝트를 반복 전환**하며 매번 패키지 조정이 발생 | **높음.** 반복적인 환경 복구를 직접 줄이는 대상 |
| Workspace와 Python 환경만 바꾸면 되고 시스템 구성은 이미 안정적 | **있음.** 다만 무거운 이미지 시스템보다 프로젝트별 환경 선택이 더 경제적일 수 있음 |
| 계속 새로운 모듈을 도입하는 개발 단계 | **중간.** 최초 호환성 해결은 남지만 실패한 변경을 되돌리는 데 유용 |
| 프로젝트마다 커널·드라이버·OS 기반 요구가 크게 다름 | **엄격한 공통 기반 방식에는 부적합.** 더 큰 단위의 분리나 부팅 환경 전환을 검토해야 함 |
| 장애 대부분이 케이블·전원·장비 상태에서 발생 | **제한적.** 소프트웨어 복원만으로 핵심 문제를 해결하지 못함 |

경제성은 개념적으로 다음과 같이 비교할 수 있습니다.

$$
\text{전환 횟수}\times
\left(
\text{기존 전환} \cdot \text{복구 비용}
-
\text{이미지 기반 전환} \cdot \text{점검 비용}
\right)
>
\text{구축 비용}+\text{유지관리 비용}
$$

여기서 전환 비용에는 파일 교체뿐 아니라 프로그램 종료, 장비 재연결, 점검, 실패 복구까지 포함해야 합니다. 마운트나 스냅샷 선택이 빠르더라도 **실험 재개까지의 전체 시간이 얼마인지**는 실제 환경에서 측정해야 합니다.

이를 판단하는 최소 검증은 거창한 범용 시스템 구축이 아니라, **실제 프로젝트 두 개의 `A → B → A` 전환 시험**입니다. 두 환경에서 사용하는 패키지·라이브러리와 장비 동작이 원래 상태로 돌아오는지, 전환 중 호스트의 공통 구성이 바뀌지 않는지, 수동 수정이 다시 필요한지를 확인하면 됩니다.

---

## 결론

**이 구상은 충분히 구현 가능하며, 이미 정상화된 프로젝트들을 반복해서 사용하는 환경에서는 유용할 수 있습니다.** “ROS가 가상환경을 허용하지 않는다”, “SDK가 시스템 경로에 설치된다”, “이미지 전환은 매번 처음부터 재설치하는 것과 같다”는 이유로 기각할 사안은 아닙니다.

다만 **임의의 `apt` 패키지까지 포함하면서 Ubuntu·ROS의 실제 의존성 전체를 전혀 바꾸지 않겠다는 조건은 항상 성립하지 않습니다.** 어떤 설치는 공통 기반과 같은 파일·라이브러리·설정에 영향을 주기 때문입니다.

현재 상황에 대한 제 판단은 다음과 같습니다.

**우선 프로젝트별로 분리 가능한 Workspace·Python·SDK 범위를 보존하는 방식은 도입 가치가 있습니다. `apt` 전역 상태까지 달라져야 한다면, 일부 폴더를 수동으로 덮어쓰는 대신 별도 루트 파일시스템이나 일관된 시스템 스냅샷으로 범위를 넓히는 편이 타당합니다. 그 범위 확대를 허용하지 않으면서 모든 프로젝트를 수용하려 하면, 그때는 요구조건 자체가 충돌할 수 있습니다.**

실제 프로젝트의 설치·변경 내역은 아직 확인하지 않았으므로, **연구실의 A와 B가 동일한 고정 기반에 들어갈 수 있는지까지 확정된 것은 아닙니다.** 하지만 “가능한 기술인가”에 대한 답은 분명히 **가능**, “쓸모가 있는가”에 대한 답은 **반복되는 환경 재구성이 실제 비용이라면 충분히 있다**입니다.