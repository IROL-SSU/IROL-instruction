# 연구실 소프트웨어 및 장비 설치·운용 문제 해결 안내

이 문서는 소프트웨어를 설치하거나 장비를 운용하는 중에 문제가 발생했을 때 무엇을 확인하고 어떻게 대응해야 하는지 설명합니다. 오류가 발생하면 곧바로 설정을 바꾸거나 프로그램을 다시 설치하지 말고, 먼저 사용 중인 장비와 소프트웨어의 정확한 버전을 확인합니다. 그다음 해당 버전의 공식 문서와 오류 메시지를 함께 확인해야 합니다.

설정 변경이나 재설치가 필요하다면 기존 데이터와 설정을 먼저 백업합니다. 공용 장비의 케이블 연결, 네트워크 구성, 로봇의 안전 설정은 다른 사용자의 작업에도 영향을 줄 수 있으므로 담당자 확인 없이 변경하지 않습니다. 무엇을 바꾸었는지 기억할 수 있도록 한 번에 하나씩 변경하고, 변경 전후의 상태를 기록하면서 문제를 확인합니다.

## 1. Ubuntu

**1.1 Ubuntu를 설치하려는데 디스크와 파티션을 구분하기 어려운 경우**

설치 화면에서 어느 디스크나 파티션을 선택해야 하는지 확실하지 않다면 설치를 진행하지 않습니다. 먼저 Ubuntu를 설치하기로 한 디스크와 설치용으로 확보한 빈 공간 또는 파티션이 무엇인지 확인합니다. 기존 운영체제나 연구 데이터가 저장된 영역은 설치 대상에서 제외해야 합니다.

특히 삭제와 포맷은 기존 데이터를 복구하기 어렵게 만들 수 있습니다. 파티션의 이름이나 크기만 보고 추측하여 선택하지 말고, 설치 대상이 맞는지 다시 확인한 뒤 진행합니다.

**1.2 NVIDIA 드라이버, CUDA 또는 cuDNN 설치 후 프로그램이 실행되지 않는 경우**

그래픽 드라이버와 CUDA·cuDNN은 각각 최신 버전을 설치한다고 해서 반드시 함께 동작하는 것이 아닙니다. 문제가 생기면 다음 항목을 순서대로 확인합니다.

1. 사용 중인 GPU의 정확한 모델을 확인합니다.
2. Ubuntu 버전과 현재 설치된 그래픽 드라이버 버전을 확인합니다.
3. 실행하려는 프로그램이 요구하는 CUDA와 cuDNN 버전을 확인합니다.
4. 확인한 버전의 조합이 서로 호환되는지 공식 문서에서 검토합니다.

설치 프로그램이 자동으로 추천한 버전이나 가장 최신 버전을 확인 없이 선택하지 않습니다. 특히 Isaac Sim·Isaac Lab을 사용할 때에는 현재 사용하는 Isaac Sim·Isaac Lab 버전이 요구하는 드라이버 조건을 확인해야 합니다. 지원되지 않는 조합에서는 설치가 완료되어도 프로그램이 정상적으로 실행되지 않을 수 있습니다. ([NVIDIA Docs][1])

**1.3 USB 장치가 보이지만 접근할 수 없다는 오류가 발생하는 경우**

USB 장치는 PC에 연결했다고 해서 바로 통신할 수 있는 것은 아닙니다. `/dev/ttyUSB*` 또는 `/dev/ttyACM*`와 같은 장치 경로는 생성되었지만 프로그램에서 열 수 없다면, 먼저 현재 사용자에게 해당 장치를 읽고 쓸 권한이 있는지 확인합니다.

권한 문제라면 장치의 소유 그룹, 사용자의 `dialout` 그룹 포함 여부, 장치별 `udev` 규칙을 확인하여 해결합니다. 원인을 확인하지 않은 채 `chmod 777`을 적용하면 모든 사용자에게 지나치게 넓은 권한을 주게 됩니다. 따라서 `chmod 777`을 기본 해결 방법으로 사용하지 않습니다. ([Arduino 도움 센터][2])

**1.4 USB 센서 하나는 동작하지만 여러 개를 연결하면 통신이 불안정한 경우**

여러 센서를 동시에 연결한 뒤 데이터가 끊기거나 장치가 반복해서 해제된다면, 프로그램 문제로 단정하기 전에 USB 연결 구성을 확인합니다. 우선 센서를 하나씩 연결하여 어떤 장치 또는 어떤 조합에서 문제가 발생하는지 확인합니다. 그다음 연결 포트, USB 허브의 전원 공급, 사용 중인 대역폭, 케이블 상태를 차례로 점검합니다.

여러 항목을 동시에 바꾸면 원인을 알기 어려우므로, 센서 수와 포트를 한 번에 하나씩 변경하면서 문제가 다시 발생하는 조건을 기록합니다.

## 2. ROS

**2.1 ROS 설치가 실패하거나 설치하려는 패키지를 찾을 수 없는 경우**

먼저 Ubuntu 버전과 설치하려는 ROS 배포판이 서로 대응하는지 확인합니다. ROS 배포판은 아무 Ubuntu 버전에나 설치하는 것이 아닙니다. 예를 들어 Ubuntu 22.04에서는 ROS 2 Humble의 공식 설치 절차를 사용할 수 있습니다. 버전 조합이 맞지 않다면 다른 설정을 수정하기 전에 운영체제와 ROS 배포판 선택부터 다시 확인합니다. ([ROS Docs][3])

**2.2 새 터미널에서 ROS 명령이나 패키지를 찾지 못하는 경우**

ROS를 설치했더라도 현재 터미널이 ROS의 실행 경로를 모르면 명령과 패키지를 찾을 수 없습니다. ROS 2 Humble을 사용하는 터미널에서는 먼저 다음 명령으로 환경 설정 파일을 불러옵니다.

```bash
source /opt/ros/humble/setup.bash
```

이 명령을 실행한 뒤 문제가 사라진다면 새 터미널에서 환경 설정이 적용되지 않았던 것입니다. Bash 기반 터미널을 열 때마다 자동으로 적용하려면 `~/.bashrc`에 해당 명령을 추가합니다.

직접 만든 워크스페이스를 사용하는 경우에는 ROS 배포판의 환경을 불러온 뒤 해당 워크스페이스의 환경 설정도 불러와야 합니다. 이때 서로 다른 ROS 배포판이나 서로 다른 워크스페이스의 설정을 한 터미널에 동시에 적용하면 예상하지 못한 패키지가 선택될 수 있습니다. 어떤 환경을 불러왔는지 확실하지 않다면 새 터미널을 열고 필요한 환경만 순서대로 다시 불러옵니다. ([ROS Docs][4])

**2.3 같은 이름의 패키지가 여러 곳에 있어 어느 코드가 사용되는지 알 수 없는 경우**

프로젝트마다 별도의 워크스페이스를 사용하고, 해당 프로젝트의 패키지 소스는 워크스페이스의 `src/` 디렉터리에 둡니다. 시스템에 `apt`로 설치된 패키지와 같은 이름의 소스 패키지를 `src/`에 추가했다면, 그것이 의도적으로 시스템 패키지를 대체하기 위한 것인지 확인합니다.

의도하지 않은 중복이라면 불필요한 패키지를 제거하거나 워크스페이스를 분리합니다. 소스 버전을 사용해야 한다면 어느 워크스페이스가 적용되는지와 환경 설정을 불러오는 순서를 명확히 관리합니다. 문제가 발생했을 때에는 패키지 이름만 확인하지 말고 실제로 어느 위치의 패키지가 선택되었는지도 확인해야 합니다. ([ROS Docs][5])

**2.4 빌드 중 의존성 패키지가 없다는 오류가 발생하는 경우**

소스 코드를 수정하기 전에 `rosdep`으로 필요한 의존성이 설치되어 있는지 확인합니다. 처음 구성하는 시스템이라면 다음 순서로 실행합니다.

```bash
sudo rosdep init
rosdep update
rosdep install --from-paths src --ignore-src -r -y
```

`rosdep init`은 시스템에서 최초 한 번만 수행합니다. 이미 초기화되어 있다는 메시지가 나오면 계속 반복하지 말고 `rosdep update`로 인덱스를 갱신합니다. 그다음 워크스페이스 루트에서 `rosdep install --from-paths src --ignore-src -r -y`를 실행하여 `src/` 아래 패키지가 요구하는 의존성을 설치합니다.

이 과정을 생략한 상태에서 발생한 빌드 오류를 바로 소스 코드 문제로 판단하지 않습니다. `rosdep`을 실행하기 전에는 현재 사용할 ROS 배포판의 환경 설정이 올바르게 적용되어 있는지도 확인합니다.

**2.5 의존성을 설치했지만 계속 빌드에 실패하는 경우**

터미널에 출력된 오류 메시지 중 처음 발생한 오류부터 확인합니다. 마지막 줄만 보고 원인을 추측하지 말고, 어느 패키지와 파일에서 실패했는지 찾습니다. 그다음 다음 항목을 확인합니다.

1. 필요한 의존성이 실제로 설치되었는지 확인합니다.
2. `package.xml`에 필요한 의존성이 선언되어 있는지 확인합니다.
3. `CMakeLists.txt` 또는 `setup.py`의 빌드·설치 설정을 확인합니다.
4. 실행하려는 노드와 스크립트가 실행 항목으로 올바르게 등록되어 있는지 확인합니다. ([ROS Docs][6])

이전 빌드 결과나 캐시가 원인으로 의심된다면 먼저 필요한 오류 로그를 보관합니다. 이후 워크스페이스의 정확한 경로를 확인한 뒤 생성된 `build/`, `install/`, `log/` 디렉터리만 정리하고 다시 빌드합니다. **`src/`를 제외한 모든 파일을 한꺼번에 삭제하는 방식은 사용하지 않습니다.** 워크스페이스 안에 별도로 보관한 설정 파일이나 데이터가 있을 수 있으므로 삭제 대상을 반드시 확인합니다.

필요한 실행 환경과 의존성이 갖춰진 Python 스크립트라면 직접 실행하여 문제를 좁힐 수 있습니다. 직접 실행했을 때 동작한다면 스크립트 자체보다 패키지 등록이나 빌드 설정을 확인해야 합니다. 다만 직접 실행은 임시 진단 방법일 뿐이며, 패키지 구성이나 빌드 설정 문제를 해결한 것은 아닙니다.

**2.6 소스 코드를 수정했는데 실행 결과가 바뀌지 않는 경우**

빌드가 성공했다는 사실과 수정한 코드가 실제로 실행된다는 사실은 서로 다릅니다. 일반적인 ROS 2 패키지 실행은 `src/`의 원본 파일이 아니라 `install/`에 설치된 실행 파일과 리소스를 사용합니다. 따라서 다음 순서로 확인합니다.

1. 수정한 패키지를 다시 빌드했는지 확인합니다.
2. 빌드한 워크스페이스의 환경 설정을 현재 터미널에서 불러왔는지 확인합니다.
3. 동일한 이름의 다른 패키지가 먼저 선택되고 있지 않은지 확인합니다.
4. 실제 실행 파일과 리소스에 수정 내용이 반영되었는지 확인합니다.

`--symlink-install`을 사용하면 일부 소스 파일과 리소스의 변경을 더 쉽게 반영할 수 있습니다. 그러나 이 옵션을 사용했다고 해서 모든 종류의 변경에서 재빌드가 불필요해지는 것은 아닙니다. ([ROS Docs][7])

**2.7 RViz에 토픽을 추가했는데 아무것도 표시되지 않는 경우**

데이터가 없다고 판단하기 전에 토픽과 좌표계를 나누어 확인합니다. 먼저 해당 토픽이 실제로 수신되는지 확인하고, RViz에서 올바른 Display 유형과 토픽을 선택했는지 확인합니다. 그다음 메시지의 `frame_id`와 RViz의 `Fixed Frame`을 비교합니다.

센서 데이터의 좌표계와 RViz가 표시 기준으로 사용하는 좌표계가 다르면 두 좌표계 사이에 유효한 TF 변환이 있어야 합니다. 예를 들어 `world` 좌표계에서 센서 데이터를 보려면 실제 센서 장착 관계에 맞는 변환을 제공해야 합니다. 화면에 보이게 만들기 위해 메시지의 프레임 이름만 바꾸거나 근거 없는 변환을 임의로 넣지 않습니다. ([ROS Docs][8])

## 3. Anaconda

**3.1 Conda 환경을 활성화한 뒤 ROS의 Python 노드나 라이브러리에서 오류가 발생하는 경우**

먼저 Conda 환경을 비활성화한 새 터미널에서 ROS만 불러와 같은 작업을 실행해 봅니다. Conda를 사용하지 않았을 때 정상 동작한다면 ROS와 Conda의 Python 환경이 섞인 것이 원인일 가능성이 있습니다.

ROS 설치 경로 아래에는 해당 ROS 배포판에서 사용하는 Python 패키지가 들어 있습니다. 각 ROS 배포판은 대응하는 Ubuntu의 시스템 Python 인터프리터와 버전을 기준으로 구성되므로, `apt`로 설치한 ROS 바이너리와 Conda의 Python 인터프리터 및 라이브러리를 한 실행 환경에서 혼용하지 않습니다. ROS를 빌드하거나 실행하는 터미널에서는 Conda 환경을 활성화하지 말고, 현재 선택된 Python 인터프리터와 라이브러리 경로가 무엇인지 확인합니다. ([ROS Docs][9])

ROS 환경을 먼저 구성하여 정상 동작을 확인하고, 그다음 Anaconda 환경을 별도로 구성합니다. 설치 순서만 지켰다고 충돌이 자동으로 방지되는 것은 아니므로 두 환경을 같은 터미널에서 섞지 않는 것이 중요합니다.

Conda 프로그램과 ROS를 반드시 연동해야 한다면 두 환경을 별도 프로세스 또는 별도 호스트로 분리하고 TCP endpoint와 같은 통신 인터페이스로 연결할 수 있습니다. 그러나 이 방식은 통신 계층과 장애 지점이 추가되므로 연구실의 기본 구성으로 권장하지 않습니다. 불가피하게 사용할 때에는 두 프로그램이 주고받는 데이터 형식과 인터페이스를 명확히 정의합니다.

**3.2 프로젝트마다 필요한 Python 패키지 버전이 달라 충돌하는 경우**

프로젝트별로 독립적인 가상환경을 생성합니다. 한 프로젝트에서 필요하다는 이유로 현재 사용 중인 다른 프로젝트의 환경에 라이브러리를 계속 추가하지 않습니다. 오류를 재현하거나 다른 사람이 환경을 이어서 사용할 수 있도록, 현재 활성화한 환경과 주요 라이브러리 버전을 명확히 관리합니다.

**3.3 Anaconda 설치 중 유료 서비스 안내가 표시되는 경우**

안내 문구만 보고 반드시 결제해야 한다고 판단하거나 단순한 후원 요청이라고 넘기지 않습니다. Anaconda의 무료 이용 범위와 유료 서비스는 적용 약관 및 학술 이용 조건을 확인하여 구분합니다. 연구실의 사용 방식이 어느 조건에 해당하는지 불분명하다면 설치 또는 결제 전에 담당자에게 확인합니다. ([Anaconda][10])

**3.4 가상환경과 모델 캐시가 저장 공간을 많이 사용하는 경우**

먼저 사용하지 않는 가상환경과 불필요한 캐시를 구분합니다. Hugging Face 모델은 Conda 가상환경 내부가 아니라 별도의 캐시 경로에 저장될 수 있습니다. 여러 환경이 같은 캐시 경로에 접근한다면 이미 다운로드한 모델을 함께 재사용할 수 있으므로, 가상환경이 다르다는 이유만으로 모델을 매번 다시 다운로드할 필요는 없습니다.

공유 캐시를 삭제하면 다른 프로젝트도 모델을 다시 받아야 할 수 있습니다. 따라서 캐시를 정리하기 전에 다른 환경이나 프로젝트에서 사용 중인지 확인합니다. ([Hugging Face][11])

## 4. UR 로봇

**4.1 로봇과 제어 PC가 네트워크로 연결되지 않는 경우**

로봇과 PC의 IP를 임의로 바꾸기 전에 어느 네트워크 인터페이스로 연결되어 있는지 확인합니다. UR 로봇의 IP는 외부 호스트의 네트워크 구성에 맞추어 할당되거나 DHCP 서버를 통해 할당됩니다. 따라서 PolyScope의 IP 설정은 PolyScope 내부에서 독립적으로 정하는 값이 아니라, 외부 네트워크의 주소 체계와 실제로 할당된 값에 의존합니다.

다음 순서로 확인합니다.

1. 현재 구성이 DHCP를 사용하는지 고정 IP를 사용하는지 확인합니다.
2. 외부 네트워크에서 사용하는 IP 주소와 Subnet 구성을 확인합니다.
3. PolyScope에 표시되거나 입력된 값이 외부 네트워크 구성과 일치하는지 확인합니다.
4. 로봇과 제어 PC가 서로 통신할 수 있도록 두 장치의 네트워크 설정을 확인합니다.

**로봇의 IP와 제어 PC의 IP를 혼동하지 않습니다.** ROS 드라이버를 실행할 때에는 로봇의 IP를 지정하고, 티치 펜던트의 External Control 설정에는 ROS 드라이버가 실행되는 PC의 IP를 입력합니다. 케이블을 분리해서 연결 인터페이스를 확인해야 한다면 먼저 로봇을 정지시킨 상태에서 수행합니다. ([유니버설 로봇 문서][12])

**4.2 네트워크 연결은 되어 있지만 ROS Controller로 로봇이 움직이지 않는 경우**

External Control URCap은 ROS bringup을 통해 ROS Controller로 로봇을 제어할 때 사용합니다. 이 방식을 사용한다면 티치 펜던트의 프로그램에 External Control 노드가 포함되어 있어야 하고, 해당 프로그램이 실제로 실행 중이어야 합니다.

로봇이 동작하지 않으면 네트워크 설정만 반복해서 바꾸지 말고 다음 두 항목을 함께 확인합니다.

1. 티치 펜던트에서 올바른 프로그램을 열었고 External Control 노드가 포함되어 있는지 확인합니다.
2. 해당 프로그램이 실행 중이며 External Control의 연결 설정이 올바른지 확인합니다.

역할을 알지 못하는 프로그램 블록을 임의로 추가하면 다른 동작이나 안전 설정에 영향을 줄 수 있으므로 담당자 확인 없이 추가하지 않습니다. ([유니버설 로봇 문서][12])

**4.3 ROS bringup과 RTDE 제어 중 어느 방식을 사용해야 하는지 혼동되는 경우**

ROS bringup은 External Control URCap과 ROS Controller를 사용하는 제어에 활용합니다. 반면 Remote Mode는 별도의 RTDE 기반 제어에 활용합니다. 먼저 실행하려는 코드가 어느 제어 방식을 전제로 작성되었는지 확인한 뒤 로봇의 실행 모드와 설정을 맞춥니다.

두 방식을 전환할 때에는 기존 제어 프로그램이 완전히 종료되었는지 확인하여 두 프로그램이 동시에 로봇을 제어하지 않도록 합니다. 실행 모드나 설정이 맞지 않는 상태에서 네트워크 설정만 바꾸지 말고, 사용 중인 제어 프로그램과 로봇 모드를 함께 확인합니다.

ROS bringup 패키지는 일반적인 운용이라면 코드 베이스를 직접 내려받아 빌드하기보다 현재 ROS 배포판에 맞는 바이너리 패키지로 설치하는 편이 안정적입니다. 소스를 수정해야 하는 명확한 이유가 있을 때에만 호환되는 브랜치와 의존성을 확인하고 별도의 워크스페이스에서 빌드합니다.

**4.4 로봇이 제한 때문에 움직이지 않거나 안전 설정을 바꾸라는 메시지가 나오는 경우**

동작을 계속하기 위해 안전 제한이나 소프트웨어의 동작 제약을 바로 완화하지 않습니다. 먼저 어떤 제한이 작동했는지와 그 제한을 변경했을 때 로봇의 움직임이 어떻게 달라지는지 확인합니다. 기능과 영향을 충분히 이해하지 못했다면 설정을 바꾸지 말고 담당자의 검토를 요청합니다.

**4.5 URCap 설치 또는 업데이트 후 기존 프로그램이 동작하지 않는 경우**

URCap을 추가하거나 업데이트하기 전에는 PolyScope 버전, 사용 중인 드라이버 버전, 기존 URCap과의 호환성을 확인해야 합니다. 최신 버전이라는 이유만으로 설치하거나 업데이트하지 않습니다.

변경이 필요하다면 기존 로봇 프로그램과 URCap 설치 설정을 먼저 백업합니다. 문제가 발생한 뒤에는 변경한 URCap 버전과 기존 구성의 호환성을 확인하고, 백업한 설정을 기준으로 변경 전 상태와 비교합니다. ([유니버설 로봇 문서][12])

## 5. MoveIt

**5.1 MoveIt 설치 중 오류가 발생하거나 패키지 구성이 불안정한 경우**

일반적인 사용에서는 현재 ROS 배포판에 맞는 `apt` 바이너리 패키지를 먼저 사용합니다. GitHub에서 소스를 내려받는 방식 자체가 잘못된 것은 아니지만, 브랜치와 의존성 버전을 사용자가 직접 맞추고 설치 구성을 계속 관리해야 합니다.

소스 수정이나 바이너리 패키지에 없는 특정 기능이 반드시 필요한 경우에만 공식 소스 빌드 절차를 따릅니다. 이때 현재 ROS 배포판과 호환되는 브랜치인지, 필요한 의존성 버전이 맞는지 확인한 후 빌드합니다. ([MoveIt][13])

**5.2 MoveIt에서 로봇 모델이나 관절 구성을 불러오지 못하는 경우**

MoveIt 구성에는 로봇의 URDF·SRDF와 관련 리소스를 제공하는 Robot Description Package가 의존성으로 필요합니다. 오류 메시지가 로봇 모델, 관절 또는 description 관련 항목을 가리킨다면 MoveIt 설정만 수정하기 전에 다음을 확인합니다.

1. 대상 로봇의 Robot Description Package가 준비되어 있는지 확인합니다.
2. 해당 패키지가 현재 ROS 환경에서 참조 가능한지 확인합니다.
3. 패키지의 로봇 모델과 관절 구성이 실제 장비 및 드라이버 구성과 일치하는지 확인합니다.

**5.3 MoveIt 문제를 해결하기 위해 코드나 설정을 수정해야 하는 경우**

공식 문서와 실제 오류 로그를 기준으로 문제가 발생한 설정과 동작 구조를 먼저 확인합니다. AI 코딩 도구가 제안한 코드나 설정은 바로 적용하지 않습니다. 각 설정이 어떤 역할을 하며 변경했을 때 무엇이 달라지는지 이해한 뒤, 필요한 부분만 수정하고 결과를 확인합니다.

## 6. RealSense

**6.1 RealSense를 USB 3.x 포트에 연결했는데 원하는 영상 설정을 사용할 수 없는 경우**

포트의 모양이나 표기만 보고 USB 3.x로 연결되었다고 판단하지 않습니다. RealSense Viewer를 열어 장치가 실제로 어떤 USB 규격으로 인식되었는지 확인합니다. 이어서 장치 정보와 영상 출력 상태를 확인하고, 필요한 해상도와 프레임률을 사용할 수 있는 연결 상태인지 점검합니다.

USB 2.x로 인식되었다면 사용할 수 있는 스트림 설정이 제한될 수 있습니다. 이 경우 프로그램의 영상 설정만 반복해서 바꾸기 전에 실제 USB 연결 규격부터 바로잡습니다. ([RealSense Help Center][14])

**6.2 RealSense가 인식되지 않거나 연결이 반복해서 끊기는 경우**

다음 순서로 한 단계씩 확인합니다.

1. 장치를 분리한 뒤 다시 연결합니다.
2. 같은 문제가 발생하면 다른 USB 포트를 사용합니다.
3. 케이블을 교체하여 케이블 문제인지 확인합니다.
4. PC를 재부팅한 뒤 다시 확인합니다.

케이블의 커넥터 모양만 보고 USB 3.x를 지원한다고 판단하지 말고 케이블 사양을 확인합니다. 위 항목을 모두 확인해도 문제가 계속된다면 사용 중인 SDK와 펌웨어의 조합, 프로그램이 출력한 오류 로그를 확인하여 원인을 좁혀갑니다.

**6.3 컬러 영상과 깊이 영상의 위치가 서로 맞지 않는 경우**

컬러 카메라와 깊이 카메라는 서로 다른 영상 기준을 사용할 수 있으므로 두 영상을 함께 처리할 때에는 SDK의 정렬 기능을 적용합니다. 화면이 맞아 보일 때까지 깊이 영상을 일정한 픽셀 수만큼 임의로 이동시키는 방식으로 해결하지 않습니다.

예를 들어 깊이 영상을 컬러 영상 좌표에 맞춰 사용할 때에는 `rs.align(rs.stream.color)`와 같은 SDK 기능을 활용할 수 있습니다. 정렬 후에도 문제가 있다면 임의의 오프셋을 추가하기 전에 어느 영상 기준으로 정렬했는지 확인합니다. ([GitHub][15])

## 7. 그리퍼 및 로봇 핸드

### Robotiq

**7.1 그리퍼를 로봇에 연결했지만 통신되지 않는 경우**

로봇 말단의 툴 커넥터를 사용하는 구성이라면 먼저 물리적 연결 상태를 확인합니다. 그다음 현재 사용하는 제어 방식에서 어떤 URCap과 통신 설정이 필요한지 확인합니다.

특히 Robotiq URCap과 툴 통신을 중계하는 URCap을 함께 사용한다면 두 URCap의 조합이 호환되는지 확인해야 합니다. 통신되지 않는다는 이유로 확인되지 않은 URCap을 추가 설치하지 말고, 현재 구성과 필요한 구성을 먼저 비교합니다. ([GitHub][16])

**7.2 그리퍼에 전원이 들어오지 않거나 시리얼 포트를 열지 못하는 경우**

ROS 드라이버에서 툴 전원을 설정하는 구성이라면 `tool_voltage`와 같은 항목을 확인합니다. 전압은 추측해서 선택하지 말고 그리퍼 매뉴얼에 명시된 정격 전압을 적용합니다.

툴 통신을 PC의 시리얼 장치로 중계한다면 PC에 생성된 장치 경로와 Robotiq 실행 패키지에 설정된 포트가 같은지 확인합니다. ROS bringup이 실행되었다는 사실만으로 툴 전원과 통신 설정이 모두 완료되었다고 판단하지 않습니다. 전원 설정과 포트 설정을 각각 확인합니다. ([GitHub][16])

### OnRobot

**7.3 OnRobot 장비를 어떤 방식으로 PC나 로봇에 연결해야 하는지 모르는 경우**

먼저 사용 중인 정확한 모델을 확인합니다. 해당 모델이 로봇과 직접 연동하는 방식을 지원하는지, 별도 컨트롤러를 통해 PC에 연결하는 방식을 지원하는지 매뉴얼에서 확인합니다.

연구실에서 이전에 사용한 PC 연결 구성은 참고할 수 있지만, 다른 모델에도 동일하게 적용된다고 가정하지 않습니다. 전원 공급장치와 통신 인터페이스는 현재 장비의 매뉴얼을 기준으로 확인하고, 기억에 의존하여 배선하거나 전원을 연결하지 않습니다.

### 로봇 핸드

**7.4 새 로봇 핸드를 처음 설치하거나 운용하는 경우**

세부 설치 및 운용 절차는 장비를 도입한 뒤 실제 검증 결과를 바탕으로 보완합니다. 처음 설정할 때에는 바로 동작 프로그램부터 실행하지 말고 전원, 통신 인터페이스, SDK, 구동 범위, 안전한 초기 자세를 먼저 확인합니다. 각 항목의 정상 동작을 확인한 뒤 다음 단계로 진행합니다.

## 8. OptiTrack

**8.1 Motive가 실행되지 않거나 라이선스를 인식하지 못하는 경우**

연구실에서 지정한 Motive 운용용 Windows PC를 사용하고 있는지 먼저 확인합니다. 그다음 설치된 Motive 버전이 연구실에서 보유한 라이선스의 지원 범위에 포함되는지 확인합니다. USB 라이선스 키가 필요한 구성이라면 키가 정상적으로 연결되어 있는지도 점검합니다.

라이선스 문제를 프로그램 재설치로 바로 해결하려 하지 말고, 사용 PC, Motive 버전, 라이선스 지원 범위, USB 키 연결 상태를 순서대로 확인합니다.

**8.2 측정 결과가 부정확하거나 마커를 안정적으로 추적하지 못하는 경우**

측정을 시작하기 전에 현재 캘리브레이션 상태를 확인합니다. 캘리브레이션이 유효하지 않거나 측정 환경이 달라졌다면 다시 수행합니다. 작업 편의를 위해 캘리브레이션은 2인이 수행하는 것을 권장하며, 세부 절차는 공식 매뉴얼과 교육 영상을 참고합니다.

캘리브레이션이 끝난 뒤 측정 목적에 맞게 마커와 Rigid Body를 구성합니다. 연구실에서는 주로 Rigid Body 기반 추적을 사용합니다. 개별 마커 데이터가 필요하다면 Rigid Body 설정만으로 해결하려 하지 말고, 개별 마커 데이터의 출력 및 처리 방법을 별도로 확인합니다.

**8.3 OptiTrack 카메라가 연결되지 않거나 전원이 들어오지 않는 경우**

PoE를 사용하는 카메라에서는 이더넷 케이블 하나가 데이터 통신과 전원 공급을 함께 담당합니다. 따라서 카메라 문제로 판단하기 전에 카메라가 지정된 스위치와 기존 연결 구성에 맞게 연결되어 있는지 확인합니다.

케이블과 스위치 포트를 임의로 바꾸면 다른 카메라의 연결에도 영향을 줄 수 있습니다. 기존 연결을 변경하지 말고, 문제가 발생하면 연구실에 보관된 OptiTrack 연구노트와 이전 해결 사례를 먼저 확인합니다.

**8.4 Motive 데이터가 ROS 쪽으로 전달되지 않는 경우**

OptiTrack을 ROS와 연동할 때에는 Motive를 실행하는 Windows Host PC와 ROS를 실행하는 Host PC가 각각 필요합니다. 두 PC는 동일한 Subnet Mask의 네트워크에 있어야 합니다. 연구실 구성은 Windows에서 TCP 방식으로 같은 서브넷의 ROS endpoint에 접근하는 방식입니다.

연결되지 않는다면 다음 항목을 순서대로 확인합니다.

1. Windows Host PC와 ROS Host PC가 각각 준비되어 있는지 확인합니다.
2. 두 PC의 IP 주소와 Subnet Mask를 확인하여 같은 서브넷에 있는지 확인합니다.
3. Windows 방화벽과 ROS Host PC의 방화벽이 통신을 막고 있지 않은지 확인합니다.
4. Windows에서 접근하는 ROS endpoint의 주소와 포트가 실제 설정과 일치하는지 확인합니다.

서로 다른 서브넷에 둔 상태에서 endpoint 설정만 반복해서 변경하거나, 기존 주소 체계를 확인하지 않고 IP를 임의로 바꾸지 않습니다. 먼저 연구실의 기존 네트워크 구성을 확인한 뒤 그 구성에 맞게 설정합니다.

## 9. Helios

**9.1 Helios에서 영상이나 깊이 데이터를 받지 못하는 경우**

먼저 사용 중인 Helios 모델에 맞는 SDK가 설치되어 있는지 확인합니다. 그다음 SDK의 기본 기능으로 영상과 깊이 데이터가 수신되는지 확인합니다. 기본 수신이 되지 않는 상태에서 로봇 연동 코드나 다른 센서와의 통합 코드부터 수정하지 않습니다.

데이터는 수신되지만 측정 품질이 좋지 않다면 렌즈와 측정 거리 조건을 확인합니다. 카메라 설치 위치를 바꾸거나 이동하면서 사용했다면 새로운 위치와 거리에서도 측정 품질을 다시 검증합니다. 로봇이나 다른 센서의 데이터와 함께 사용하면서 위치가 맞지 않는다면 필요한 좌표계 보정이 수행되었는지 확인합니다.

**9.2 이더넷을 연결했는데 장치가 동작하지 않는 경우**

연구실에서는 Helios 장비용으로 지정된 PCIe 네트워크 카드의 포트를 사용합니다. PCIe 카드는 PC 메인보드의 확장 슬롯에 장착된 장치이며, PC의 모든 이더넷 포트가 같은 용도로 설정되어 있는 것은 아닙니다. 장비용 카드와 포트를 구분하기 어렵다면 임의의 포트에 연결하지 말고 담당자에게 확인합니다.

연결 가능한 인터페이스와 전원 공급 방식은 Helios의 세부 모델과 구성에 따라 달라질 수 있습니다. 따라서 다른 포트로 옮기기 전에 현재 모델의 요구사항을 확인합니다. 이더넷 케이블이 연결되었다는 사실만으로 장치에 필요한 전원까지 공급되고 있다고 판단하지 않습니다. ([루시드 지원센터][17])

[1]: https://docs.nvidia.com/deeplearning/cudnn/installation/latest/prerequisites.html?utm_source=chatgpt.com "Prerequisites — NVIDIA cuDNN Installation"
[2]: https://support.arduino.cc/hc/en-us/articles/360016495679-Fix-port-access-on-Linux "Fix port access on Linux – Arduino Help Center"
[3]: https://docs.ros.org/en/humble/Installation/Ubuntu-Install-Debs.html?utm_source=chatgpt.com "Ubuntu (deb packages) — ROS 2 Documentation"
[4]: https://docs.ros.org/en/humble/Tutorials/Beginner-CLI-Tools/Configuring-ROS2-Environment.html?utm_source=chatgpt.com "Configuring environment — ROS 2 Documentation: Humble ..."
[5]: https://docs.ros.org/en/humble/Tutorials/Beginner-Client-Libraries/Creating-A-Workspace/Creating-A-Workspace.html?utm_source=chatgpt.com "Creating a workspace — ROS 2 Documentation"
[6]: https://docs.ros.org/en/humble/Tutorials/Beginner-Client-Libraries/Creating-Your-First-ROS2-Package.html?utm_source=chatgpt.com "Creating a package — ROS 2 Documentation: Humble documentation"
[7]: https://docs.ros.org/en/humble/Tutorials/Beginner-Client-Libraries/Colcon-Tutorial.html?utm_source=chatgpt.com "Using colcon to build packages — ROS 2 Documentation"
[8]: https://docs.ros.org/en/humble/Tutorials/Intermediate/RViz/RViz-User-Guide/RViz-User-Guide.html?utm_source=chatgpt.com "RViz User Guide — ROS 2 Documentation: Humble documentation"
[9]: https://docs.ros.org/en/humble/How-To-Guides/Using-Python-Packages.html?utm_source=chatgpt.com "Using Python Packages with ROS 2 - ROS documentation"
[10]: https://www.anaconda.com/legal/terms/terms-of-service?utm_source=chatgpt.com "Terms of Service - Anaconda"
[11]: https://huggingface.co/docs/huggingface_hub/en/package_reference/environment_variables?utm_source=chatgpt.com "Environment variables - Hugging Face"
[12]: https://docs.universal-robots.com/Universal_Robots_ROS2_Documentation/doc/ur_client_library/doc/setup/robot_setup.html?utm_source=chatgpt.com "Robot setup — Universal Robots ROS 2 Driver Documentation 0.1 ..."
[13]: https://moveit.ai/install-moveit2/binary/?utm_source=chatgpt.com "MoveIt 2 Binary Install"
[14]: https://support.realsenseai.com/hc/en-us/community/posts/15149975945363-realsense-SDK-on-windows-11?utm_source=chatgpt.com "realsense SDK on windows 11"
[15]: https://github.com/IntelRealSense/librealsense/blob/master/wrappers/python/examples/align-depth2color.py?utm_source=chatgpt.com "align-depth2color.py - realsenseai/librealsense - GitHub"
[16]: https://github.com/UniversalRobots/Universal_Robots_ROS2_Driver/blob/main/ur_robot_driver/doc/setup_tool_communication.rst "Universal_Robots_ROS2_Driver/ur_robot_driver/doc/setup_tool_communication.rst at main · UniversalRobots/Universal_Robots_ROS2_Driver · GitHub"
[17]: https://support.thinklucid.com/getting-started/ "Getting Started with LUCID Cameras | LUCID Support & Help"
